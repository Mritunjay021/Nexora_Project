# Client environment variables (copy to client/.env or set in your environment)

# Base URL for the backend API. Example for local dev:
VITE_BACKEND_URL=http://localhost:5000

# You can add other VITE_... variables here if you need to expose more values to the client.
